import {
  API_GATEWAY,
  BACKEND_CANISTER_ID,
  DERIVATION_ORIGIN,
  DFX_NETWORK,
  IDENTITY_PROVIDER,
  IS_MAINNET,
} from './environment.common';

export const ENV = {
  API_GATEWAY,
  BACKEND_CANISTER_ID,
  DFX_NETWORK,
  IDENTITY_PROVIDER,
  IS_MAINNET,
  DERIVATION_ORIGIN,
};
