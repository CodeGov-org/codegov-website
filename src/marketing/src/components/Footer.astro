---
import { getGlobalConfigStory } from '../storyblok/api';

const {
  content: { footer_links },
} = await getGlobalConfigStory();
---

<footer class="footer">
  <div class="footer-main">
    {
      footer_links.map(linkCategory => (
        <div class="footer-link-category-container">
          <h2 class="footer-link-category-title">{linkCategory.title}</h2>

          <ul class="footer-link-category">
            {linkCategory.children.map(link => (
              <li>
                <a href={link.content.cached_url} class="footer-link">
                  {link.title}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))
    }
  </div>

  <div class="footer-copyright">
    <div class="footer-copyright-content">
      <span class="footer-copyright-text">
        © {new Date().getFullYear()} CodeGov™. All Rights Reserved.
      </span>
    </div>
  </div>
</footer>
